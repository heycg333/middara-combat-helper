# Middara Combat Helper V2.6B

Upload these extracted files to the GitHub Pages repository root. Do not upload the ZIP file itself. After deploy, open the app on the tablet, use Data -> hosted/installable web app -> Update app cache, then reload.
